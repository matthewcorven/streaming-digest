# Squad Decisions

## Active Decisions

- 2026-07-18: Initial squad roster set to Morpheus, Trinity, Tank, Dozer, Neo, Switch, Scribe, and Ralph.
- 2026-07-18: Initial casting universe set to The Matrix for this repository.
- 2026-07-18: Frontend scope is hosted Blazor WASM without server-side rendering.
- 2026-07-18: UI component library is Microsoft Fluent UI Blazor (`Microsoft.FluentUI.AspNetCore.Components` NuGet package). Blazor WASM without SSR, served as static files from the ASP.NET Core 10 API project, communicating exclusively via HTTP API calls. No other CSS framework needed. The `fluentui-blazor` skill is available for development reference. (By: Trinity, via Matthew Corven — user directive)
- 2026-07-18: Streaming Digest's Blazor WASM UI is a Progressive Web App from day one of UI implementation, not retrofitted. MVP scope: web app manifest, installability, app icons/splash (incl. maskable), service worker registration for lifecycle/install (not offline), responsive mobile-first layout, and platform-appropriate UX using PWA capabilities (display-mode detection, share target, shortcuts) as needed. Full offline mode — offline data caching, offline search, background sync of user actions — is explicitly MVP+, with the MVP service worker foundation laid so it's an incremental addition. The `pwa-development` skill is the authoritative reference for PWA patterns (preferred over assumptions); https://whatpwacando.today/ is the per-platform capability reference. Declared in ARCHITECTURE.md (target runtime line, §1 goals, §2.1, §5.2 PWA subsection); compatible with the no-SSR static hosting model since the WASM project publishes service-worker.js and manifest.json as static assets served by the API. (By: Trinity, via Matthew Corven — user request)
- 2026-07-18: Trinity's PWA-first UI declaration in ARCHITECTURE.md (§1, §2.1, §5.2) reviewed and approved as architecturally consistent with existing Fluent UI / no-SSR WASM / static hosting declarations and PRD.md. Follow-ups closed by coordinator: PWA task items added to IMPLEMENTATION_PLAN.md (Task 0.2 baseline assets bullet, new Task 2.3c "Establish PWA baseline"); service-worker update flow + stale WASM asset edge case added to UPGRADE_PATHS.md (§3.1, §3.3). (By: Morpheus — review verdict)
- 2026-07-18: Design guidance lives in `.copilot/skills/fluent-design-principles` — a platform-agnostic UI/UX design-principles skill distilled from the Fluent 2 design language (color, typography, elevation, motion, layout, shapes, iconography, accessibility, content design, design tokens). Implementation guidance stays in the `fluentui-blazor` skill (Fluent* Blazor components, NuGet package). Routing rule: UI/UX principle questions → `fluent-design-principles`; Fluent* component/NuGet questions → `fluentui-blazor`. Boundary documented in the new skill's frontmatter `DO NOT USE FOR` clause. (By: Coordinator, via Matthew Corven — user request)
- 2026-07-18: Presentation layer design documented. Full presentation layer spec at docs/presentation/PRESENTATION.md covering app shell, all 8 pages, 6 components, interaction patterns (SSE, keyboard, URL state, theme), empty/error states, and PWA specifics. Desktop-primary, left nav, bento dashboard, hybrid search layout, wizard+checklist onboarding, inline accordion cards, 5-tab edit modal, modal-only notes, full keyboard shortcuts, SSE real-time updates, URL search state, system theme with override, Fluent UI breakpoints, branded login, actionable empty states, tabbed settings, PWA shortcuts (Search/Digest/Run Ingestion). Ground-up design from user requirements, use cases, and workflows; all decisions confirmed interactively with Matthew Corven. (By: Trinity, via Matthew Corven — interactive design session)
- 2026-07-18: Presentation layer doc review verdict: APPROVED WITH NOTES. Morpheus reviewed docs/presentation/PRESENTATION.md against PRD, ARCHITECTURE, API_SPEC, DATA_MODEL. The doc is the authoritative presentation layer reference: all 8 pages, 6 components, interaction patterns (SSE/keyboard/URL/theme), empty+error states, and PWA specifics are specified with desktop+mobile ASCII wireframes. Every hard-MVP UI requirement from PRD §2.5/§2.6/§2.7/§2.10 maps to a spec; architecture constraints (no-SSR WASM static hosting, Fluent UI, PWA day-one, SSE not SignalR) are respected; API endpoint paths and response shapes (video cluster, original/override/effective, paged results, operations) match API_SPEC verbatim; data model entities (videos, segments, notes, field_override_history) are correctly represented. One correction applied in place: §3.3 post-login routing precedence now includes PRD §2.10's "last selected mode" step. (By: Morpheus — review verdict)
- 2026-07-18: Squad governance amended so agents classify decisions before writing to the decisions inbox. ARCHITECTURAL decisions (hard to reverse, real trade-off, future reader needs the "why") get a full ADR at `docs/adr/NNNN-<slug>.md` (format per `.agents/skills/domain-modeling/ADR-FORMAT.md`, next unused NNNN) plus a one-line pointer in the inbox (`ADR-NNNN: <title> — see docs/adr/NNNN-<slug>.md (By: {agent})`). TEAM/PROCESS/SCOPE decisions (roster, casting, routing, review verdicts, user directives, governance) continue to go to the inbox as full text. `.squad/decisions.md` remains the single index of ALL decisions; architectural rationale lives exactly once, in the ADR. Scribe preserves pointer lines verbatim on merge and commits `docs/adr/` alongside `.squad/`. Applies going forward — existing ADRs 0001–0010 and existing decisions.md entries are not renumbered or moved. (By: Morpheus, via Matthew Corven — user request, TEAM/PROCESS decision)
- 2026-07-19: `docs/implementation/IMPLEMENTATION_PLAN.md` adopts two standing conventions: (1) per-task `Source:` anchor lines — one line immediately after each `### Task X.Y: Title` heading, formatted `Source: \`docs/...\` §...; ADR-XXXX (if applicable)`, anchoring the task to its governing doc section(s) and ADR(s); new tasks must include one. (2) Convergence milestones M1–M4 — a `### Convergence milestones` subsection at the tail of `## Implementation sequencing` defining mid-plan pass/fail scenarios (Given/When/Then, gated on cumulative slices: M1 after slice 4, M2 after 8, M3 after 11, M4 after 13) that validate converging workstreams before Phase 19 end-to-end acceptance; the Quality gates list requires M1–M4 to pass at their declared slices. Keeps the plan traceable to source docs/ADRs and catches cross-workstream integration regressions earlier than final acceptance. Future plan edits must preserve both conventions. (By: Morpheus, via Matthew Corven — user-approved edit list, TEAM/PROCESS decision)
- 2026-07-19: Implementation plan review resolutions — six open plan gaps resolved in `docs/implementation/IMPLEMENTATION_PLAN.md`: (1) new Task 12.5a owns run-scoped Digest assembly/storage (DATA_MODEL §3.37; ADR-0006, ADR-0012); dashboard (12.6) and Matrix (14.3) render from the stored artifact. (2) New Task 5.5 owns the `unavailable` terminal ingestion state (DATA_MODEL §3.6); the redundant stopgap bullet was removed from Task 6.1 verification. (3) Task 12.7 capture affordance demoted to MVP+ — the recall harness stays hard-MVP, but dataset growth in MVP is file-based (edit the golden dataset file directly); the in-UI "this should have ranked" capture/review queue is MVP+. (4) Task 0.4 fixture library stays up-front (status quo, lowest churn). (5) Tasks 7.3 and 17.5 split — 7.3 keeps LLM refinement, new 7.3a owns segment regeneration cutover; 17.5 keeps upgrade/migration policy, new 17.5a owns the Upgrade & Maintenance admin panel. Tasks 2.4+2.5 left intact (no split). (6) Task 16.2 contextual admin actions distributed to their owning slices via a placement map; sequencing slice 13 updated to cite 16.1/16.3 only. The "known gaps under discussion" bullet was removed from Quality gates as all listed gaps are now closed. (By: Morpheus, via Matthew Corven — user-approved edit list, TEAM/PROCESS decision)
- 2026-07-24: Implementation plan depth/evidence resolutions — second user-approved edit pass to `docs/implementation/IMPLEMENTATION_PLAN.md`: (A) new Task 1.5 owns a domain event type catalog — single catalog of `event_type` values for `domain_events` (code-level enum/constants + docs listing), seeded with event types implied by the plan; a convention test fails on uncatalogued values. (B) New `## Verification evidence` convention — durable verification results (benchmarks, recall reports, cross-platform checks, restore dry-runs, prototype comparisons) committed under `docs/verification/` as `{task-id}-{slug}.md` (prose + environment + date + outcome) with machine-readable JSON alongside for numeric results; append-only so regressions are visible; quality gates citing measured targets (12.7 recall, 12.8 latency) are not met until the evidence artifact is committed; evidence bullets appended to Tasks 12.7, 12.8, 17.1, 17.4, and the Quality gates list. (C) Task 15.1 gains automated observability smoke assertions — an integration test asserts trace spans and structured log fields (incl. correlation ID) through the OTLP pipeline, not only manual dashboard checks. (1) New Task 7.4 prototypes ffmpeg frame extraction (reusing Task 6.4 temp-media lifecycle) vs yt-dlp frame extraction against the test fixture, evaluated on quality/accuracy, size/encode cost, speed, toolchain footprint (macOS ARM, Windows ARM, Linux), temp-media fit, failure modes, and container complexity; the decision is recorded in an ADR (next available number); the screenshot-generation task is renumbered 7.4 → 7.5 and follows the ADR. (2) Whisper forced to HTTP service — Task 6.3 contract is an internal HTTP service matching API_SPEC §21; a CLI engine (whisper.cpp preferred) is wrapped inside the service as an implementation detail. (3) Task 6.4 owns all pipeline temp media — retitled "Implement temporary media lifecycle and transcription fallback"; owns the shared temp-media lifecycle (quota, filename scheme, cleanup) for transcription, screenshot frame extraction (7.5), and any future stage. (4) Task 10.1a gains a typed scraper client matching the internal scraper API contract (API_SPEC §20), including health-check integration. (5) New Task 4.6 — concurrency/race conformance harness: failing-before/passing-after tests for race-prone invariants (parallel retries, single-active-generation unique index, one-active-transcript under concurrent cutovers, near-simultaneous digest assembly, outbox double-dispatch); harness plus retry/unique-index scenarios land in Phase 4, later-feature scenarios land with their features; sequencing slice 2 updated to cite it. NOTE: the security conformance layer item from the same review remains unresolved — the user did not ratify it; do not add it. (By: Morpheus, via Matthew Corven — user-approved edit list, TEAM/PROCESS decision)

- 2026-07-24: Security conformance resolved as Task 2.6 harness — final parked review item. New Task 2.6 in `docs/implementation/IMPLEMENTATION_PLAN.md` establishes a security conformance test harness covering: Hangfire dashboard (`/admin/jobs`) auth rejection, screenshot endpoint (`/api/screenshots/{id}`) auth, path-traversal rejection on screenshot/media file serving, internal service endpoints (`/internal/matrix/*`, `/internal/scrape/*`, `/internal/audio-to-text/*`) unreachable via the public surface, diagnostics endpoints (e.g. `GET /api/config/runtime`) never leaking secret values, and CSRF rejection of token-less mutations beyond login (extending Task 2.2). The harness pattern matches the existing API conformance harness (Task 2.3b) and concurrency harness (Task 4.6); scenario tests that depend on later-phase endpoints land alongside those owning phases, and the harness runs in CI from Phase 2 onward. Sequencing slice 2 updated to cite Task 2.6. This closes the last parked item from the implementation plan review series — no open parked items remain. (By: Morpheus, via Matthew Corven — user-approved edit, TEAM/PROCESS decision)
- 2026-07-24: Prototype policy + vector prototype tasks 11.3a/11.3b. **Prototype policy (user directive):** all prototypes use synthetic, programmatically generated data only — no AI-generated content, no latency or token cost, and a controlled/known content profile so results are attributable to the approach under test. **Task 11.3a — Prototype vector knowledge-base approach:** synthetic corpus generator plus synthetic bulk embeddings with a real-embedding validation subset; validates document construction, staleness derivation, ADR-0004 per-video duplication of shared resource documents, cluster aggregates, and the pgvector HNSW vs IVFFlat index trade-off. **Task 11.3b — Prototype vector user-search approach:** synthetic query generator; validates hybrid scoring, cluster aggregation, relativeSimilarityPercent, high-signal matching, and related items; explores ranking weight ranges ahead of Task 12.3. **Roles in plan:** prototypes run after 11.3 in slice 4; the 11.3a corpus generator seeds the Task 12.8 recall-harness dataset; 11.3b findings feed Task 12.3 ranking weight defaults. Open implementation decisions updated: HNSW vs IVFFlat informed by 11.3a; ranking weight defaults informed by 11.3b. Related ADRs (e.g., ADR-0004 duplication, ADR-0012 similarity scale) remain conditional on prototype outcome. (By: Matthew Corven — user directive, TEAM/SCOPE decision)
- 2026-07-24: User directive — all prototypes run as early as possible (ideally first) so costly pivots surface while cheap. Implementation: prototypes are sequencing slice 2, immediately after foundation; downstream tasks revalidate against prototype findings. (By: Matthew Corven via Copilot)
- 2026-07-24: Implementation plan migrated to GitHub Issues and file retired. All 94 tasks from `docs/implementation/IMPLEMENTATION_PLAN.md` now live as GitHub issues (#1–#94) in matthewcorven/streaming-digest, each titled `[Task X.Y] <title>` with full task content (Source anchors, requirements, verification) in the body, a migration header (task/phase/slice), and labels: `squad`, `squad:{member}` (deterministic routing from the plan's member map), `phase-{N}`, `slice-{N}`, plus `kind:prototype` on 7.4/11.3a/11.3b. Standing conventions established during plan review survive the file's deletion and now attach to issues: (1) `Source:` anchor lines are preserved in each issue body and required on new task issues; (2) convergence milestones M1–M4 remain MVP quality gates (tracked by `slice-*` labels: M1 after slice 5, M2 after 9, M3 after 12, M4 after 14); (3) the Verification evidence convention (`docs/verification/`, append-only, JSON for numeric results) is unchanged; (4) the prototype policy (synthetic programmatic data, no AI, no token cost) is unchanged. The `squad-issue-assign` and `squad-triage` workflows (`issues: [labeled]` triggers) were temporarily disabled during the bulk import to stop them re-labeling issues against the manifest, then re-enabled after labels were enforced. `docs/implementation/IMPLEMENTATION_PLAN.md` was deleted; README.md and PRESENTATION.md references now point at the issues. Slice/member mapping is recoverable from issue labels and the git history of the retired plan file. (By: Coordinator, via Matthew Corven — user request)
- 2026-07-25: Persistent per-agent/task model preferences saved to `.squad/config.json`: defaultModel (all code implementation) claude-sonnet-4.6; Morpheus (Lead) claude-opus-4.7; Switch (tests), Ralph, Scribe mai-code-1-flash; prototyping work (kind:prototype tasks) claude-opus-4.6 (taskModelOverrides.prototype). Precedence at spawn: prototype task → opus-4.6; otherwise per-agent override; else defaultModel. (By: Coordinator, via Matthew Corven — user request, TEAM/PROCESS decision)
- 2026-07-25: Task 11.3a vector knowledge-base prototype PROVEN all five claims under test against the real Aspire + pgvector stack (Postgres 17.10, pgvector 0.8.5; 500 videos, 11,958 vectors, dim=384). Two outcomes: (1) **HNSW recommended as MVP default index** over IVFFlat — measured HNSW build 979 ms / query 0.522 ms / recall@10 0.99 / 23.9 MB vs IVFFlat (lists=100, probes=10) build 422 ms / query 0.509 ms / recall 0.98 / 19.6 MB; HNSW wins on incremental build (no pre-population requirement), no `lists`/`probes` tuning, and recall headroom; cost ~2.3× build time and ~1.22× index size, negligible at MVP scale. (2) **The pgvector AppHost image pin is production-needed** — stock `postgres` has no pgvector extension; pin `.WithImage("pgvector/pgvector","pg17").WithImageRegistry("docker.io")` in `src/StreamingDigest.AppHost/AppHost.cs` before any Task 11.3 embedding work, promoted via normal PR. Data-volume gotcha: named volume persists across image changes; if initialized by stock `postgres:18.x`, `pg17` fails initdb — `docker volume rm streamingdigest-postgres-data` first. Boundary: synthetic recall/latency numbers are internal-consistency checks only and do NOT transfer to a real embedding model; re-measure in Task 11.3. NOTE: Neo's original "no ADR" call on the HNSW default was OVERTURNED by Morpheus — see ADR-0016 entry below. Evidence: `docs/verification/11.3a-vector-knowledge-base.md` + `.json`. (By: Neo, issue #18, PR #96 — merged)
- 2026-07-25: Task 11.3b vector user-search prototype — no ADR filed (confirmed by Morpheus review): the DATA_MODEL §6 ranking formula stands as written; all mechanical ranking targets PROVEN against real PostgreSQL + pgvector (hybrid textWeight/vectorWeight blend over ts_rank_cd+trgm and pgvector cosine; cluster aggregation 0.65·max + 0.25·avg(top3) + 0.10·coverage; relativeSimilarityPercent min-max over the PRE-pagination top-200 pool — do NOT normalize post-pagination; note boost 0.08 and interaction boost min(0.05, 0.01×count) bounded; related-item discovery via nearest cluster centroids). INCONCLUSIVE by construction (needs the real Task 11.3 embedding provider): paraphrase/vague-query semantic recall, and the ADR-0012 high-signal ABSOLUTE-cosine threshold VALUE — keep the mechanism, defer the numeric threshold to real-model calibration (sweep 0.70–0.98). Recommended Task 12.3 starting weights: textWeight/vectorWeight 0.65/0.35 (sweep 0.35/0.65 → 0.80/0.20); cluster max/avg-top3/coverage 0.65/0.25/0.10 (max 0.55–0.75, coverage ≈ 0.10); note_boost 0.08 (0.05–0.12); interaction cap 0.03–0.08. **Production-relevant finding:** hybrid latency ~447 ms/query vs 11.3a's 0.522 ms pure-vector — per-query tsvector construction + ts_rank_cd + GROUP BY over 12k docs; hybrid scoring needs materialized tsvector columns (and ideally pre-aggregated per-video state), feeding Task 12.3 design; Task 12.8 owns re-measurement. Npgsql hard-won notes: `NpgsqlConnection.GlobalTypeMapper.UseVector()` + `GetFieldValue<Pgvector.Vector>`; the `<=>` operator needs a typed `Pgvector.Vector` parameter (bare `float[]` binds as `real[]` → SQLSTATE 42883). Evidence: `docs/verification/11.3b-vector-user-search.md` + `.json`. (By: Neo, issue #19, PR #97 — merged)
- 2026-07-25: ADR-0015: Screenshot extraction uses ffmpeg directly on the temp-media file, in the worker — see docs/adr/0015-screenshot-extraction-approach.md. Task 7.4 prototype disproved the plan's ffmpeg-vs-yt-dlp framing: yt-dlp has no native frame extraction — every frame-level path delegates to ffmpeg (proven by byte-identical PSNR); reframed as extract-local vs download-then-extract. Worker runs ffmpeg directly against the Task 6.4 temp-media file, encoding WebP via libwebp at segment start + configurable offset (default +5s); ffmpeg (not Python/yt-dlp) baked into the worker image (+543 MB single apt layer; Debian build has libwebp natively). All 8 evaluation axes PROVEN. Closes the "screenshot extraction approach" open implementation decision; Task 7.5 follows this ADR. Evidence: `docs/verification/7.4-screenshot-extraction.md` + `.json`. (By: Dozer, issue #84, PR #98 — merged)

- 2026-07-26: Ralph handoff and implementation delegation directive. Ralph is to inspect the authoritative issue queue, determine the next ready implementation issue, and delegate implementation work to `mai-code-1-flash-picker` subagents. Each implementation subagent must create or switch to the relevant issue branch, open a draft PR linked to the issue, inspect surrounding existing implementations before coding, request coordinator validation before ready-for-review, and only then mark the PR ready. Recorded in `.squad/decisions/inbox/squad-ralph-handoff-implementation-delegation-0746a58f.md`. (By: Scribe, via Matthew Corven — user directive)
- 2026-07-25: ADR-0016 (required by Morpheus ruling, not yet authored): MVP vector index is HNSW, not IVFFlat — see pending `docs/adr/0016-vector-index-hnsw.md`. Morpheus OVERTURNED 11.3a's "no ADR" call: HNSW meets the ADR bar because DATA_MODEL §3.22 explicitly left the choice open, the migration is hard to reverse, and a real measured trade-off exists; Task 11.3's implementer needs a recorded decision, not a prototype findings section. (By: Morpheus — prototype synthesis review, PR #99)
- 2026-07-25: Prototype synthesis (11.3a + 11.3b + 7.4) — Lead rulings: all technical verdicts CONFIRMED (11.3a 5/5; 11.3b 5 mechanisms + 2 honest inconclusives; 7.4 8/8 plus the disproven ffmpeg-vs-yt-dlp framing — a success of the prototype process); preserve the PROVEN-mechanism vs PROVEN-value distinction everywhere (11.3b proved plumbing, not tuning). Rulings: (1) 447 ms hybrid latency is an implementation threat only — Task 12.3 hard-requirement: materialized tsvector generated column + GIN, not per-query tsvector; §6 formula unchanged; Task 12.8 owns re-measurement against ≤2 s P50 @ 500 videos. (2) ADR-0012 threshold deferral ACCEPTED but has no landing task — new issue needed: with the real Task 11.3 provider, measure the own-vs-best-other cosine gap distribution, sweep 0.70–0.98, set `search.highSignalThresholdPercent` in the gap, commit evidence per the Verification convention; `## Depends On: 11.3`; gate before Task 12.5a digest rendering is trusted. (3) +543 MB ffmpeg in worker ACCEPTABLE — ARCHITECTURE §2.2/§9.1 presuppose it; in-process validated over sidecar. (4) +1-frame (~30 ms) non-keyframe offset DOES NOT MATTER — screenshots never load-bearing; absence → placeholder. (5) Real-model evidence owed: recall@k/MRR (Task 12.7), threshold gap (new issue), cluster weight differentiation + cross-topic separation (12.7/12.3), HNSW at 2k videos (12.8); ADR-0013 covers recall but NOT threshold calibration. (6) Spike code on main STAYS as primary-source evidence — `spikes/README.md` convention: throwaway, excluded from slnx, evidence-linked, never imported by production. **Requested coordinator follow-up actions (not yet done):** author `docs/adr/0016-vector-index-hnsw.md` (assertion in synthesis §6.2); apply DATA_MODEL edits (§3.22 name HNSW; §6 add materialized-tsvector implementation note; §3.21 index bullet consistency); apply ARCHITECTURE edits (§4.5 search-flow implementation note; §12 Homebrew-vs-Debian ffmpeg skew note); create the ADR-0012 calibration issue and link #18/#19; comment the real-model re-verification list onto #17 (Task 11.3). Evidence: `docs/verification/prototype-synthesis-11.3a-11.3b-7.4.md`. (By: Morpheus, PR #99 — open, pending user review)
- 2026-07-24: GitHub issues are now the authoritative source of truth for issue execution. Agents must read each issue's body for `## Depends On` and `## Blocked By` sections, use those sections as the source of truth for dependencies and blockers, and treat the issue tracker as the live ordering queue. `.squad/task-manifest.json` is retired as an execution source and removed from the repository. (By: Coordinator, via Matthew Corven — user directive)
- 2026-07-25: `scripts/issue_queue.py` readiness fix (TEAM/PROCESS, bug fix — no ADR): `--state` default changed `open`→`all` so CLOSED dependency referents resolve as satisfied instead of reading as blockers; `--state open` kept as opt-in legacy view; `--mode status` board counts unchanged; a `(missing)` marker now distinguishes an unresolvable referent from an open one. Evidence `docs/verification/101-issue-queue-readiness.md`. (By: Dozer, issue #101)
- 2026-07-25: ADR-0017: Phase-gate Task X.0 references denote previous-phase completion, not gated launches — see docs/adr/0017-phase-gate-task-x0-references.md (By: Dozer, issue #101). A bare `X.0` depends-on ref is a migration artifact meaning "previous phase's last task"; the 17 affected issues were rewritten to reference that real issue, and the `(missing)` marker is kept as the permanent safety net. Not mass-unblocked — all 17 remain behind real OPEN issues.

## Governance

- All meaningful changes require team consensus
- Document architectural decisions here
- Keep history focused on work, decisions focused on direction

- 2026-07-26: Issue #43 final validation gate recorded for Trinity's implementation on branch `matthewcorven-issue-43-add-ui-observability-links`. Draft PR #121 passed the Switch review (targeted unit/integration coverage passed) and received Morpheus approval as aligned with issue scope and repo guidance. Caveat: the Hangfire link depends on deployment exposing a working dashboard at the configured URL; acceptable for issue scope. Coordinator instructed Trinity to mark PR #121 ready for review. (By: Scribe, via Matthew Corven — validation gate)

- 2026-07-26: Issue #43 final PR state recorded: PR #121 is open, not a draft, and its body includes the accepted Hangfire caveat. (By: Scribe, via Matthew Corven — PR finalization)

- 2026-07-26: Issue #43 and PR #121 are closed. (By: Scribe, via Matthew Corven — closure)

- 2026-07-26: Issue #55 / PR #122 review artifact rejected and reassigned. Morpheus verdict: REJECT; Switch verdict: FAIL. Root cause: the PR branch on GitHub remains the scaffold commit with an empty diff while the actual auth changes exist only as uncommitted local worktree changes, so the reviewable artifact does not satisfy issue #55. Reviewer lockout applied: Tank may not revise the rejected artifact. Revision reassigned to Trinity. (By: Scribe, via Matthew Corven — review outcome)

- 2026-07-26: Issue #55 / PR #122 revision cycle reassigned to Rai under the existing lockout. Tank and Trinity remain locked out. Required fix scope: enforce forced-password-change gating before protected endpoints, remove non-production auth bypasses, and add/update auth integration coverage for login/logout/password change/unauthenticated mutation rejection. Coordinator opened a revision session for Rai on branch `matthewcorven-issue-55-auth-flow-hardening` using `mai-code-1-flash-picker`. (By: Scribe, via Matthew Corven — revision reassignment)

- 2026-07-26: Issue #55 final validation gate recorded for PR #122 after Rai's revision cycle: Switch verdict PASS; Morpheus verdict APPROVE. Accepted caveat: the new auth integration coverage depends on Docker/Postgres being available in CI/runtime. (By: Scribe, via Matthew Corven — validation gate)

- 2026-07-26: Issue #44 final validation gate recorded for PR #123: Switch verdict PASS; Morpheus verdict APPROVE. Accepted caveat: end-to-end purge of screenshots/raw HTML debug captures depends on future writers registering `media_artifacts` rows; not a blocker. (By: Scribe, via Matthew Corven — validation gate)

- 2026-07-26: Issue #44/PR #123 is open and ready for review, with the accepted `media_artifacts` caveat preserved in the PR body notes. (By: Scribe, via Matthew Corven — PR state)

- 2026-07-26: Issue #87 final validation gate recorded for PR #124: Switch verdict PASS; Morpheus verdict APPROVE. Accepted caveat: existing stored URL values will be rewritten to canonical forms when entities are assigned or reloaded. (By: Scribe, via Matthew Corven — validation gate)

- 2026-07-26: Issue #87/PR #124 is open and ready for review, with the approved canonicalization caveat preserved in the PR body notes. (By: Scribe, via Matthew Corven — PR state)

- 2026-07-26: Issue #45 final validation gate recorded for PR #125: Switch verdict PASS; Morpheus verdict APPROVE. Accepted caveat: other subsystems are not yet wired to these concurrency knobs, and the public settings/docs surface still does not enumerate the new keys. (By: Scribe, via Matthew Corven — validation gate)

- 2026-07-26: Issue #45/PR #125 is open and ready for review, with the approved concurrency-scope caveat preserved in the PR body notes. (By: Scribe, via Matthew Corven — PR state)

- 2026-07-26: Issue #88 final validation gate recorded for PR #126: Switch verdict PASS; Morpheus verdict APPROVE. Accepted caveat: this is intentionally heuristic-only for the MVP rule-based slice and does not yet incorporate LLM-based classification or user-correction examples. (By: Scribe, via Matthew Corven — validation gate)
