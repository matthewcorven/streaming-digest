# Squad Decisions

## Active Decisions

- 2026-07-18: Initial squad roster set to Morpheus, Trinity, Tank, Dozer, Neo, Switch, Scribe, and Ralph.
- 2026-07-18: Initial casting universe set to The Matrix for this repository.
- 2026-07-18: Frontend scope is hosted Blazor WASM without server-side rendering.
- 2026-07-18: UI component library is Microsoft Fluent UI Blazor (`Microsoft.FluentUI.AspNetCore.Components`) with static-file WASM served from the ASP.NET Core 10 API and HTTP-only client/server communication. `fluentui-blazor` remains the implementation reference. (By: Trinity, via Matthew Corven — user directive)
- 2026-07-18: Streaming Digest's Blazor WASM UI is a Progressive Web App from day one of UI implementation. MVP scope includes manifest, installability, icons/splash, lifecycle-only service worker, responsive mobile-first layout, and platform-appropriate PWA UX; full offline behavior is explicitly MVP+. (By: Trinity, via Matthew Corven — user request)
- 2026-07-18: Morpheus reviewed Trinity's PWA-first declaration and approved it as consistent with existing Fluent UI / no-SSR / static-hosting decisions; follow-ups were closed in IMPLEMENTATION_PLAN.md and UPGRADE_PATHS.md. (By: Morpheus — review verdict)
- 2026-07-18: UI/UX principle questions route to `.copilot/skills/fluent-design-principles`; Fluent Blazor component/package questions route to `fluentui-blazor`. (By: Coordinator, via Matthew Corven — user request)
- 2026-07-18: Presentation layer design is specified in `docs/presentation/PRESENTATION.md` and is the authoritative presentation-layer reference after Morpheus's approval-with-notes review. Post-login routing precedence was corrected to include PRD §2.10's last-selected-mode step. (By: Trinity and Morpheus, via Matthew Corven)
- 2026-07-18: Squad governance requires decision classification before inbox write. Architectural decisions get full ADRs under `docs/adr/NNNN-<slug>.md` plus one-line pointers in this file; team/process/scope decisions stay here as full text. `.squad/decisions.md` remains the single index of all decisions. (By: Morpheus, via Matthew Corven — user request)
- 2026-07-19: `docs/implementation/IMPLEMENTATION_PLAN.md` adopted two standing conventions: per-task `Source:` anchors immediately under each task heading, and convergence milestones M1–M4 as Given/When/Then pass/fail gates at cumulative slices. Future plan/task issue edits preserve both conventions. (By: Morpheus, via Matthew Corven — user-approved edit list)
- 2026-07-19: Implementation-plan review resolutions closed six gaps: Task 12.5a owns run-scoped digest assembly/storage; Task 5.5 owns unavailable-video lifecycle; Task 12.7's UI capture affordance is MVP+ while the file-based recall harness remains MVP; Task 0.4 stays early; Tasks 7.3/7.3a and 17.5/17.5a are split; Task 16.2 contextual admin actions are distributed to owning slices. (By: Morpheus, via Matthew Corven — user-approved edit list)
- 2026-07-24: Second implementation-plan depth/evidence pass added Task 1.5 domain-event catalog, the append-only `docs/verification/` evidence convention, observability smoke assertions in 15.1, screenshot prototype Task 7.4 with screenshot generation renumbered to 7.5, Whisper as an internal HTTP service, Task 6.4 ownership of shared temp media, typed scraper client work in 10.1a, and Task 4.6 concurrency/race conformance harness. (By: Morpheus, via Matthew Corven — user-approved edit list)
- 2026-07-24: Security conformance is a named Phase-2 harness (Task 2.6) covering dashboard auth, screenshot auth, traversal rejection, internal endpoint posture, secret non-leakage, and CSRF rejection of token-less mutations. (By: Morpheus, via Matthew Corven — user-approved edit)
- 2026-07-24: Prototype policy: all prototypes use synthetic, programmatically generated data only — no AI-generated content, no latency/token cost, and a controlled content profile. Tasks 11.3a and 11.3b validate the vector knowledge-base and vector user-search approaches respectively. (By: Matthew Corven — user directive)
- 2026-07-24: User directive — run prototypes as early as possible; implementation sequencing moved the prototype work to slice 2 so expensive pivots surface while cheap. (By: Matthew Corven via Copilot)
- 2026-07-24: GitHub issues are the authoritative execution queue. `docs/implementation/IMPLEMENTATION_PLAN.md` was retired after migration to issues, and agents must use each issue body's `## Depends On` / `## Blocked By` sections as the dependency/blocker source of truth. (By: Coordinator, via Matthew Corven — user directive)
- 2026-07-25: Persistent per-agent/task model preferences were saved to `.squad/config.json`: defaultModel = claude-sonnet-4.6; Morpheus = claude-opus-4.7; Switch/Ralph/Scribe = mai-code-1-flash; prototype work = claude-opus-4.6. Spawn precedence is prototype override, then per-agent override, then defaultModel. (By: Coordinator, via Matthew Corven — user request)
- 2026-07-25: Task 11.3a vector knowledge-base prototype proved all five claims under test against the real Aspire + pgvector stack; HNSW is the recommended MVP index over IVFFlat, and the AppHost must pin `pgvector/pgvector:pg17`. Synthetic recall/latency findings are internal-consistency checks only and must be re-measured with the real embedding provider. (By: Neo, issue #18, PR #96 — merged)
- 2026-07-25: Task 11.3b vector user-search prototype confirmed DATA_MODEL §6's ranking mechanics and related-item behavior without changing the formula. The threshold value for ADR-0012 remains deferred to real-model calibration, and hybrid search must use materialized tsvector state rather than per-query construction. (By: Neo, issue #19, PR #97 — merged)
- 2026-07-25: ADR-0015: screenshot extraction uses ffmpeg directly on the temp-media file in the worker. The Task 7.4 prototype showed yt-dlp is not a true frame-extraction alternative, and ffmpeg stays the chosen implementation path for Task 7.5. (By: Dozer, issue #84, PR #98 — merged)
- 2026-07-25: ADR-0016 is required: MVP vector index is HNSW, not IVFFlat. Morpheus overturned the earlier "no ADR" call because DATA_MODEL §3.22 left the choice open and the measured trade-off is hard to reverse. (By: Morpheus — prototype synthesis review, PR #99)
- 2026-07-25: Prototype synthesis rulings for 11.3a + 11.3b + 7.4: preserve the PROVEN-mechanism vs PROVEN-value distinction; treat the 447 ms hybrid-latency result as an implementation warning, not a design verdict; keep threshold-value calibration as a real-model follow-up; accept the ffmpeg worker-size trade-off and the non-keyframe +1-frame screenshot offset; and keep spike code on main as evidence under the `spikes/README.md` convention. Requested follow-ups remain authoring ADR-0016, updating DATA_MODEL/ARCHITECTURE notes, creating the ADR-0012 calibration issue, and carrying the re-verification list into Task 11.3 follow-up work. (By: Morpheus, PR #99 — open at time of ruling)
- 2026-07-25: `scripts/issue_queue.py` readiness fix: the helper now defaults `--state` to `all` so CLOSED dependency referents resolve as satisfied, and `(missing)` now marks unresolvable references in text output. (By: Dozer, issue #101)
- 2026-07-25: ADR-0017 final rule: bare `Task X.0` dependency references mean the real upstream data/capability dependency; if no upstream issue exists, the head issue has no dependency. This superseded the earlier previous-phase interpretation, and the affected issues were rewritten accordingly. (By: Dozer, issue #101)
- 2026-07-28: Independent re-review verdict — PR #179 ([Task 12.2] vector search SQL) is ready for maintainer review after Tank's revision commit `80c983e`; upgrade-path migration safety for the legacy materialized-view shape was confirmed. (By: Morpheus)
- 2026-07-28: Independent re-review verdict — PR #181 ([Task 12.x] ADR-0012 threshold calibration) is ready for maintainer review after Dozer's revision commit `19b67a8ab321c2f259390a3b70cfc7d40d9a66af`; fresh installs seed 70 and upgrade paths preserve stored values unless changed deliberately. (By: Morpheus)
- 2026-07-28: Final independent re-review verdict — PR #180 ([Task 11.4] Store embeddings in pgvector) is ready for maintainer review after Morpheus's revision commit `4b34e19`; prior blockers on ingestion path, host wiring, DbContext mapping, and regression coverage were resolved. (By: Switch)
- 2026-07-31: Prefer Playwright-based end-to-end test development in C# rather than JavaScript. (By: Matthew Corven via Copilot — user directive)
- 2026-07-31: Place the Playwright C# end-to-end project under `tests/StreamingDigest.E2E/`, not `src/StreamingDigest.E2E/`. (By: Matthew Corven via Copilot — user correction)
- 2026-08-01: When there is no corpus, dashboard/search/ingestion pages show only the empty-state experience, and the only available action is adding channels. No ingestion/backfill/review-runs affordances should appear in that state. (By: Matthew Corven via Copilot — user directive)
- 2026-08-01: For runtime startup failures, prevent what can be prevented proactively and emit notification/signaling for the remainder instead of silently degrading. (By: Matthew Corven via Copilot — user directive)
- 2026-08-02: Ralph's charter now codifies a named sub-agent dispatch protocol: read the member charter/history first, use the cast name as task name, include the role emoji in descriptions, and inline full identity/runtime context in every spawn; adversarial-review spawns follow the same protocol. (By: squad-coordinator)
- 2026-08-02: Initial hypothesis that `create_session` ignored model overrides was superseded and marked resolved. The apparent defect was coordinator schema misuse plus a silent-fallback footgun: top-level `create_session.model` is dropped while `kickoff.model` is honored. (By: Squad-Coordinator; references af38092e, f4991a55)
- 2026-08-02: Controlled probe `c3e716b8` confirmed the root cause and fix: `create_session` honors the override only inside `kickoff.model`; top-level placement falls back to the coordinator default model. Squad docs/config were updated to document both tool shapes and the placement footgun, and post-fix Morpheus spawns reported kimi-k3 as intended. (By: Squad-Coordinator)

## Governance

- All meaningful changes require team consensus.
- Document architectural decisions here.
- Keep history focused on work, and decisions focused on direction.
- 2026-07-25: Ralph commits per issue. (By: Matthew Corven via Copilot — user request)
- 2026-07-25: Ralph delegates implementation and does not implement directly. (By: Matthew Corven via Copilot — user request)
- 2026-07-25: Session model directive — use MAI-Code-1-Flash for subagents while Ralph works the queue. (By: Matthew Corven via Copilot — user request)
- 2026-07-26: Always delegate implementation to `mai-code-1-flash`; subagents commit and raise PRs only after evaluating their successful, fully validated scope against founding documents. (By: Matthew Corven — user directive)
- 2026-07-26: The queue helper must not return an issue as available when that issue already has a linked PR. (By: Matthew Corven — user request)
- 2026-07-26: Ralph's handoff protocol — inspect the authoritative queue, choose the next ready implementation issue, and delegate to `mai-code-1-flash-picker` subagents. Each subagent must work on the issue branch, open a draft PR, inspect surrounding implementations, request coordinator validation before ready-for-review, then mark ready. (By: Matthew Corven via Ralph — user directive)
- 2026-07-26: Issue #55 / PR #122 first review verdict — REJECT/FAIL because the GitHub PR branch still contained only the scaffold commit while the real auth work lived uncommitted in a local worktree. Tank was locked out of revisions and the artifact was reassigned. (By: Matthew Corven via Coordinator and Morpheus)
- 2026-07-26: Issue #55 / PR #122 revision ownership moved to Rai under the existing reviewer lockout; Tank and Trinity remain locked out. Required fixes were forced-password-change gating, removal of non-production auth bypasses, and auth integration coverage for login/logout/password change/unauthenticated mutations. (By: Morpheus)
- 2026-07-26: Issue #55 / PR #122 final validation gate — Switch PASS, Morpheus APPROVE. Accepted caveat: the auth integration coverage depends on Docker/Postgres availability in CI/runtime. (By: Matthew Corven — user directive)
- 2026-07-27: PR descriptions must include implementation details, automated testing detail, and an explicit link to the related GitHub issue. (By: Matthew Corven — user directive)