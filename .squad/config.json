{
  "version": 1,
  "defaultModel": "openai/gpt-5.6-luna-pro",
  "agentModelOverrides": {
    "morpheus": "moonshotai/kimi-k3",
    "scribe": "openai/gpt-5.6-luna",
    "ralph": "z-ai/glm-5.2"
  },
  "modelIdFormatNote": "Values here are SHORT IDs (human-readable). At spawn time, the coordinator resolves each to its full prefixed form via the procedure in .squad/templates/model-selection-reference.md ('Resolving valid model IDs'). If a spawn fails with 'model provider not found' or 'Model not available', re-resolve and retry — do NOT edit this file to add prefixes."
}
