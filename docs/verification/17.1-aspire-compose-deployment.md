# Verification: Aspire-to-Compose deployment

> Evidence note: this report records what was actually executed in the current environment and what remains an analysis-only expectation for other hosts.

## 1. Summary

The Compose deployment artifacts for Task 17.1 were validated from the repository root with the documented startup flow:

- `docker compose config`
- `docker compose build api worker scraper`
- `docker compose up -d`

The compose stack now resolves to a single-project deployment named `streaming-digest` with deterministic container names matching the `streaming-digest-*` convention.

## 2. Host verification results

| Host / scenario | Status | Evidence |
| --- | --- | --- |
| macOS ARM | ✅ Measured | Ran `docker compose config`, `docker compose build api worker scraper`, and `docker compose up -d` from the current macOS ARM environment. The deployment resolved successfully and the compose services were created with the expected `streaming-digest-*` naming. |
| Windows ARM | ⚠️ Analysis only | Not executed on a Windows ARM host in this session. The configuration is compatible with standard Docker Compose semantics and the images used are multi-arch-friendly, but runtime behavior on Windows ARM remains unmeasured here. |
| CPU fallback | ✅ Design/operationally compatible | The stack does not require GPU-specific configuration; the deployment remains CPU-compatible by default because the compose services use standard container images and the local model runtime is configured through Ollama rather than GPU-only assumptions. |

## 3. Startup checklist

- [x] Compose project/base name is `streaming-digest`.
- [x] Services resolve under deterministic `streaming-digest-*` container names.
- [x] A single command starts the deployment stack: `docker compose up -d`.
- [x] Internal-only service networking is preserved for PostgreSQL and the observability/internal services.
- [x] API/worker/service wiring points at Compose service names (`postgres`, `scraper`, `ollama`, `otel-collector`).

## 4. Notes for future hosts

- A fresh host should still pull the required images and, if needed, provision the Ollama model payloads before the full stack is fully usable.
- The documented startup command is still the right entry point; the runtime verification above confirms it resolves in the current environment.
